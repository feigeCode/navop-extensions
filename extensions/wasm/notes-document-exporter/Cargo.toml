[package]
name = "notes_document_exporter_wasm"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
pulldown-cmark = "0.12.2"
wit-bindgen = "0.57.1"
zip = { version = "6.0.0", default-features = false }
