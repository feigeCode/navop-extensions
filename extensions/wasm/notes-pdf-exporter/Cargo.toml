[package]
name = "notes_pdf_exporter_wasm"
version = "0.1.0"
edition.workspace = true
publish.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
notes_document_export_core = { path = "../notes-document-export-core", features = ["pdf"] }
wit-bindgen = "0.57.1"
